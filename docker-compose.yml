services:
  server:
    build: ./server
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    restart: unless-stopped

  web:
    build: ./web
    env_file: .env
    depends_on:
      - server
    ports:
      - "5173:80"
    restart: unless-stopped
